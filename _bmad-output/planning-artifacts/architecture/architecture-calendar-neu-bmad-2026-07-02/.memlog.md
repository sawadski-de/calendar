---
topic: Architektur-Spine für Team-Terminkalender mit Synchronisation
goal: Architecture Spine auf Basis der finalen PRD (prd-calendar-neu-bmad-2026-07-02) und UX-Spine (DESIGN.md/EXPERIENCE.md) erarbeiten — Invarianten, die eine konsistente Epic-/Story-Aufteilung und Umsetzung ermöglichen
updated: 2026-07-02T00:00
---

- (override by coach) memlog.py konnte nicht über `uv run` oder `python` ausgeführt werden (weder uv noch ein funktionierender Python-Interpreter auf diesem System verfügbar) — .memlog.md wird für diesen gesamten Lauf von Hand im selben Format gepflegt (init + jeder append manuell nachgebildet), analog zum Vorgehen im PRD-Lauf
- (event) Intent: Create einer neuen Architecture Spine; kein bestehender Lauf unter {planning_artifacts}/architecture gefunden, Ordner neu angelegt
- (note) Quelle: prd.md + addendum.md (prd-calendar-neu-bmad-2026-07-02), DESIGN.md + EXPERIENCE.md (ux-calendar-neu-bmad-2026-07-02), brief.md + addendum.md (brief-calendar-neu-bmad-2026-07-02) gelesen und extrahiert; kein project-context.md vorhanden (echtes Greenfield, kein bestehender Code)
- (decision by user) Arbeitsmodus: Coaching Path
- (decision by user) Deliverable-Scope: Die Spine selbst reicht als alleiniges Artefakt, keine zusätzliche menschenlesbare Rendering-Ebene nötig
- (decision by user) Dokumentsprache: Deutsch (konsistent mit brief.md/prd.md/DESIGN.md/EXPERIENCE.md), abweichend von config.yaml document_output_language: English — bewusste Nutzerentscheidung, analog zur PRD-Entscheidung
- (decision by user) Stack-Grundsatzentscheidung: Alles containerisiert (Docker), Frontend Angular, Backend .NET, Datenbank PostgreSQL
- (note by coach) Aus PRD/UX abgeleitete Constraints unabhängig von Sprachwahl: separater Hintergrund-Sync-Prozess neben dem Web-Server nötig (Polling alle paar Minuten), Backup-/Restore-Pflicht für native Termine (Must-Have-NFR), verschlüsselte Ablage der OAuth-Tokens (Must-Have-NFR)
- (decision by user) Paradigma: Layered/Clean Architecture (Domain → Application → Infrastructure → API), inkl. Ports&Adapters für Kalender-Provider und einem einzigen zentralen Application-Service für die Privat-Default-Filterung aller Termin-Leseoperationen
- (decision by user) Login-Mechanismus: Entkoppelt von Kalender-OAuth — E-Mail+Passwort via ASP.NET Core Identity, Accounts werden von Dennis als Admin angelegt (kein Self-Signup), begründet durch die UX-Entscheidung, dass Kalender-Verbindung optional/zeitlich entkoppelt vom Login ist
- (decision by user) Sync-Worker-Hosting: separater Worker-Container (nicht die empfohlene eingebettete Variante) — eigener Docker-Service fürs Outlook-/Google-Polling, unabhängig von der API deploybar
- (note by coach) Konsequenz aus separatem Worker-Container: Worker und API müssen dieselbe Domain-/Infrastructure-Bibliothek referenzieren (Status-Heuristik, Dedup-Regeln), damit keine Logik dupliziert/divergiert — Koordination ausschließlich über die gemeinsame Postgres-DB, kein direktes RPC zwischen Worker und API nötig
- (decision by user) Status-Berechnung (FR-10): vorausberechnet bei jedem Schreibzugriff (nativer Create/Update UND Sync-Upsert), als gespeichertes Feld am Termin — ein einziger Domain-Service, von API und Worker gleichermaßen aufgerufen; Lesezugriffe lesen nur das gespeicherte Feld
- (decision by user) Heuristik-Kalibrierung (löst PRD Offene Frage #3): kein Teilnehmer → immer unterbrechbar; mit Teilnehmer(n) ≤45 Min. → unterbrechbar; ganztägig ODER (≥90 Min. UND ≥3 Teilnehmer) → bitte-nicht-stören; alles dazwischen → unterbrechbar; Tageszeit fließt nicht ein
- (decision by user) Backup-Strategie (löst PRD Offene Frage #4): tägliches pg_dump via Cron-Sidecar-Container, 14 Tage rollierende Aufbewahrung auf Docker-Volume desselben Hosts; kein Schutz gegen Totalausfall des Hosts (akzeptiert)
- (decision by user) Deprovisionierung (PRD Offene Frage #5): wird jetzt entschieden, nicht länger offen gelassen
- (decision by user) Deprovisionierung: Soft-Deaktivierung — Konto/Login/Sync gesperrt, OAuth-Tokens gelöscht, Person verschwindet aus Personenauswahl; native Termine und Teilnahmen bleiben erhalten (kein Hard-Delete)
- (note by coach) PRD Offene Frage #6 (Widerruf des Kalenderzugriffs durch den Nutzer selbst) wird durch die ohnehin geplante Sync-Transparenz-NFR abgedeckt — ein Token-Widerruf zeigt sich beim nächsten Poll-Versuch als Sync-Fehler wie jeder andere Auth-Fehler auch; kein zusätzlicher Mechanismus nötig
- (note by coach) PRD Offene Fragen #7 (Zeitzone) und #8 (externe Teilnehmerdaten) bleiben wie im PRD als Annahme/akzeptiertes Risiko bestehen, keine Architekturentscheidung nötig
- (note by coach) Web-Recherche aktuelle Versionen (2026-07-02): .NET 10 ist aktuelles LTS (Support bis Nov 2028); Angular 22 aktuelle stabile Version (veröffentlicht 2026-06-03); PostgreSQL 18.4 aktuelle stabile Major-Version
- (decision by coach) Ergänzende technische Entscheidungen ohne eigene Nutzer-Rückfrage getroffen (Standard-Ops-Entscheidungen, kein Produkt-Trade-off): Reverse Proxy Caddy (automatisches TLS, minimaler Konfigurationsaufwand für Ein-Personen-Betrieb) mit pfadbasiertem Routing (gleiche Origin für Angular-Static und /api), Cookie-basierte Session-Auth (HttpOnly/Secure/SameSite) statt Bearer-Token im Client-Storage, Backend liefert nur maschinenlesbare Fehlercodes (kein lokalisierter Text), gesamte Lokalisierung liegt im Angular-Frontend
- (event) Coaching-Runde abgeschlossen — Paradigma, Stack, Login, Worker-Hosting, Status-Berechnung, Heuristik-Kalibrierung, Backup, Deprovisionierung entschieden; weiter zur Distillation der Spine
- (event) ARCHITECTURE-SPINE.md geschrieben (Status: final) — 14 ADs, Consistency Conventions, Stack (aktuelle Versionen web-recherchiert), Structural Seed (Container-Diagramm inkl. Deployment, ERD, Source Tree), Capability→Architecture Map (FR-1…FR-15), Deferred-Sektion
- (note by coach) lint_spine.py (mechanischer Reviewer-Gate-Schritt) konnte mangels Python/uv nicht ausgeführt werden — Reviewer-Gate läuft nur mit Rubrik-Walker + den beiden konfigurierten finalize_reviewers (Versions-Recherche-Check, Adversär-Inkompatibilitäts-Check) plus Input-Reconciliation gegen PRD/UX/Brief; keine mechanische Lint-Prüfung auf Duplikat-IDs/Platzhalter
- (event) Reviewer-Gate abgeschlossen: Reconcile-PRD (reale Lücken: Sync-Transparenz fehlt komplett, Restore-Prozess fehlt, FR-7-Schreibschutz nicht strukturell erzwungen, FR-10-Overlap-Tie-Break fehlt, FR-9-Ausnahme-Scope mehrdeutig), Reconcile-UX (Sync-Transparenz + Admin-Rollen-Durchsetzung fehlen, kleinere Punkte), Reconcile-Brief (clean, keine Widersprüche), Rubrik (kritische Lücke: Sync-Transparenz/Rollenmodell fehlen strukturell, Restore fehlt, Stack-Tabelle inkonsistent gepinnt), Versions-Check (alle Versionen aktuell und korrekt: .NET 10 LTS, Angular 22, PostgreSQL 18; Hinweis: Angular 22 erst ~4 Wochen alt, Angular 21 LTS wäre reifere Alternative — kein Fehler, bewusste Abwägung wert), Adversär (kritisch: AD-5/AD-6 Enum-Case-Mismatch, AD-2/AD-7 Delta-vs-Snapshot-Widerspruch, AD-7 fehlende Null-Konvention für native Termine; hoch: kein Bulk-Read-Vertrag für Mehrpersonen-Ansicht, importierte Serientermine nicht wirklich adressiert; mittel: fehlender einheitlicher CurrentStatusService, unbewachter Token-Refresh-Schreibzugriff, Deaktivierung vs. laufender Sync-Zyklus)
- (event) Alle kritischen und hohen Reviewer-Funde behoben: AD-16 (Sync-Status-Felder + Sichtbarkeit) und AD-17 (serverseitige Rollen-Durchsetzung) neu ergänzt; AD-14 um Restore-Runbook erweitert; AD-2 um Vollsnapshot-Semantik und strukturellen Schreibschutz (kein Write-Interface) erweitert; AD-7 um partiellen Unique-Index (NULL statt Platzhalter) erweitert; AD-5/AD-6 auf einen gemeinsamen PascalCase-Enum vereinheitlicht; AD-3 um Bulk-Read-Methode für Mehrpersonen-Ansicht sowie um die explizite Entscheidung erweitert, dass die FR-9-Teilnehmer-Ausnahme für native und synchronisierte Termine gleichermaßen gilt (löst PRD-Mehrdeutigkeit); AD-15 neu ergänzt (importierte Serientermine pro Instanz normalisiert, providerseitige Instanz-Event-ID); AD-6 um benannten CurrentStatusService und Overlap-Tie-Break erweitert; AD-8 um Token-Refresh-Eigentümerschaft (nur Worker) erweitert; AD-12 um Deaktivierung-vs-laufender-Zyklus-Regel erweitert; Stack-Tabelle konsistent gepinnt (Docker Compose v2, Caddy v2, Serilog v4); Structural Seed um Single-Environment-Hinweis erweitert
- (note) Angular-22-Frische-Hinweis des Versions-Reviewers bewusst nicht architektonisch behoben, sondern dem Nutzer zur Kenntnis gegeben (kein Blocker, reine Abwägungsfrage Angular 22 vs. 21 LTS)
- (event) spine finalized
