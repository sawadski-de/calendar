---
topic: UX for calendar-neu-bmad
updated: 2026-07-02T14:10
---

- (event) UX workspace created; sources confirmed: brief.md, brief addendum.md, prd.md, prd addendum.md
- (event) source extraction subagent completed; report captured below as notes
- (note) product: self-hosted unified team calendar aggregating each member's Outlook/Google calendar into one view, modeled on a unified email inbox, plus a derived interruptibility signal (unterbrechbar vs. bitte-nicht-stören); success = daily use replacing Outlook
- (note) team: fixed 5-10 people including Dennis; Dennis is product owner, developer AND operator (bus factor 1)
- (note) only named persona/journey is Mara (UJ-1): checks a colleague's derived status in Day view side-by-side columns before interrupting, without opening the appointment; can manually override own status if wrongly shown do-not-disturb
- (note) form factor confirmed explicit in sources: responsive web app only, browser-based, self-hosted, no native app, must work on mobile via browser
- (note) core views: own calendar Month/Week/Day (no reload on switch); multi-person view = user-selected teammates side-by-side in columns (not overlaid), Day/Week only, selection unspecified whether Month is in/out of scope
- (note) privacy default: non-owned appointments show only "privat/beschäftigt" + status, never title/attendees/location, unless viewer is a listed attendee; enforced server-side; addendum clarifies this is a deliberate display rule, not a data-access restriction (full data is read/stored internally)
- (note) status derivation: automatic heuristic from duration/attendee-count/time-of-day; zero-attendee = always interruptible; manual override always available and persists until user changes it; no visual difference shown between auto vs. manual status
- (note) must-have NFR: sync transparency — UI must show last-successful-sync timestamp per calendar and surface repeated/silent sync failures; sync is one-way import only (never writes back), latency is "a few minutes", never real-time
- (note) must-have: login/access protection required (mechanism unspecified)
- (gap) no visual identity/branding material anywhere in sources (no colors, logo, typography, tone) — must ask user directly
- (gap) no existing design system / component library referenced in sources — unclear if reuse or fresh
- (gap) no accessibility requirements stated in sources
- (gap) multi-person view behavior on small screens explicitly left open in PRD (horizontal scroll vs. cap on selection)
- (gap) status override UI mechanism unspecified (toggle vs. menu vs. duration-based DND)
- (gap) UI language not stated (all docs are German)
- (gap) onboarding/OAuth-connection flow has no described UX, including consent-denial failure state
- (gap) notification UX for reminders (Should-Have) unspecified
- (gap) no journey described for appointment-creation, slot-finder, onboarding, or operator/Dennis sync-health monitoring view
- (gap) color-coding by calendar source is only a Could-Have with no detail
- (note) addendum: closest market analogs are Clockwise (auto-derives focus time, sets DND status) and Reclaim.ai (redacts titles to generic "Busy" across synced calendars) — neither combines cross-provider view + colleague-facing interruptibility signal
- (note) addendum: FR-10 status thresholds are provisional interpolations from a 5-example table Dennis gave, not user-confirmed exact numbers — worth treating as tunable in any status-explainer UI
- (decision by user) working mode: coaching path (walk decisions together, use creative tools)
- (decision by user) stakes: consumer-quality polish expected even though usage is internal (Dennis wants it to feel like a well-crafted product, not a bare-bones prototype)
- (decision by user) visual identity starts from zero — no existing brand/tool to mirror; will use creative tools (color themes, design directions) to explore
- (decision by user) UI language: real i18n with a language switcher (at least German + English), not a single fixed language
- (decision by user) dark mode: yes, full light + dark from the start
- (decision by user) multi-person day view density: airy/calm over information-dense — generous whitespace, prioritize a relaxed feel over cramming max columns/appointments in view, even if it means scrolling
- (decision by user) tone/personality: ruhig/zurückhaltend (calm, reserved); should feel like a modern, high-usability, well-crafted web application; no specific reference tools named
- (decision by user) accessibility floor: formal WCAG 2.1 AA target
- (decision by user) reminder notifications: browser push + in-app banner/toast (email not selected)
- (event) dispatched design-directions creative tool (4 directions) for the Day-view multi-person hero screen, rendering to .working/
- (decision by user) multi-person view extends to Month view too (PRD FR-2 only specified Day/Week — this is a UX-phase scope extension beyond the PRD, worth flagging back to product); side-by-side columns don't fit month grid cells, so mechanism needs its own design (e.g. compact per-person status indicators per day cell) — to be explored, not yet decided
- (decision by user) chosen visual direction: Slate Dark (dark-mode-native, cool slate palette, muted teal/rose glow-bar status encoding, cyan accent, rounded-xl corners, IBM Plex Sans-style type) — see .working/direction-slate-dark.html
- (decision by user) appointment creation: double-click an empty time slot opens the create form with that time pre-filled; a separate "+ Neuer Termin" button also opens the same form but with no time pre-selected (user picks time manually in the form)
- (decision by user) calendar connection (Outlook/Google) is optional and done later via a settings page, not a forced onboarding step after first login; new users land directly in their (empty) calendar
- (decision by user) per-user connection UI: simple list, one row per provider (Outlook/Google) — connected/not-connected, "Verbinden" button, last-sync timestamp, error indicator if consent/connection fails
- (decision by user) Dennis (operator) needs a dedicated admin overview showing sync status for ALL team members at a glance (person | provider | status | last sync) — a distinct surface from the per-user settings page
- (decision by user) wants a wireframe exploring month-view multi-person status mechanism before deciding (dots vs. bars vs. click-popover)
- (decision by user) confirmed no further discovery input needed — ready to move toward distillation once the wireframe decision lands
- (event) dispatched Excalidraw wireframe (3 options) for month-view multi-person status mechanism, rendering to .working/
- (decision by user) month-view multi-person mechanism: Option C — day cell stays clean with only a subtle aggregate marker if anyone is DND; click opens a small popover listing each selected teammate's name + status
- (decision by user) status-override control: icon-button + flyout menu ("Status ändern"), as proposed in the Slate Dark direction
- (decision by user) real role system with permission checks (Admin vs. member) — not just an unguarded extra page — since it may grow beyond Dennis-only later; admin sync overview is gated behind the Admin role
- (event) discovery closed; moving to Finalize
- (event) DESIGN.md and EXPERIENCE.md distilled in full from memlog + chosen Slate Dark direction + wireframe decision + sources; self-check passed (FR/UJ references verbatim, token/component symmetry, token references resolve, canonical section order)
- (note) remaining open marker: small-screen multi-person behavior — spine proposes horizontal-scroll-with-pinned-own-column as [ASSUMPTION]/[OPEN QUESTION] since PRD §7 left it explicitly unresolved
- (decision by user) reviewer gate: run rubric walker + accessibility ad-hoc reviewer before polish
- (decision by user) additional key-screen mocks wanted: Month view + popover, Settings/Verbindungen, Admin sync overview
- (event) 3 key-screen mocks rendered to .working/: key-month-view.html, key-settings-connections.html, key-admin-sync-overview.html
- (event) rubric review complete (review-rubric.md): adequate-not-clean; 3 high findings — person-selector component entirely unspecified, contrast targets missing for base text/muted-on-surface pairs, contradiction between State Patterns and Responsive&Platform on whether there's a hard 3-teammate cap
- (event) accessibility review complete (review-accessibility.md): 1 critical — sync-failure errorText (raw dnd on surface) computes ~4.33:1, fails AA 4.5:1 text threshold; 2 high — no keyboard equivalent for double-click-create, no focus-trap/restore spec for flyout/popovers; several medium (hairline border contrast, accent-light link-text contrast ~3.35:1, no reduced-motion mention, fixed flyout width risky for translated strings, no aria-live for sync failure)
- (decision by user) person-selector mechanism: avatar chip row for currently-selected teammates + a "+" that opens a searchable dropdown to add more
- (decision by user) no hard cap on teammate selection — unlimited, horizontal scroll if needed (resolves the rubric-flagged contradiction; the "wähle bis zu 3 weitere" copy was wrong and must be corrected)
- (event) fix pass applied: person-selector spec added, cap contradiction resolved, base-text contrast targets added, critical sync-error contrast fixed (dnd-text token, 9.40:1 dark / 8.77:1 light), keyboard/focus-trap/reduced-motion/aria-live specs added, view-switcher and language-switcher components added, .working/ artifacts linked inline; remaining open questions narrowed to narrow-viewport column behavior and 12h/24h time-gutter format (neither decided in discovery)
- (decision by user) additional mock coverage wanted: appointment creation form, login page, person-selector dropdown (expanded state)
- (event) 3 more key-screen mocks rendered: key-appointment-create.html, key-login.html, key-person-selector.html
- (event) working artifacts promoted: 7 mocks → mockups/, 1 wireframe → wireframes/; inline .working/ links in both spines updated to mockups//wireframes/ paths; non-chosen direction explorations left in .working/ as audit trail
- (event) structural editorial review applied to both spines: cut true-duplicate content (Colors "Avoid" line, Layout&Spacing rationale restatement, EXPERIENCE.md intro blockquote, State Patterns audit-trail narrative); no QUESTION items required human input
- (event) prose editorial review applied: unified terminology (Sync-Transparenz, appointment-create entry points, pre-filled/pre-selected hyphenation) across both spines; no other issues found
- (event) spines finalized: DESIGN.md and EXPERIENCE.md set to status: final, updated: 2026-07-02
